import './test_plan_new_item.html';
import { Template } from 'meteor/templating';

/**
 * Template Helpers
 */
Template.TestPlanNewItem.helpers({});

/**
 * Template Event Handlers
 */
Template.TestPlanNewItem.events({});

/**
 * Template Created
 */
Template.TestPlanNewItem.onCreated(() => {
  
});

/**
 * Template Rendered
 */
Template.TestPlanNewItem.onRendered(() => {
  
});

/**
 * Template Destroyed
 */
Template.TestPlanNewItem.onDestroyed(() => {
  
});
