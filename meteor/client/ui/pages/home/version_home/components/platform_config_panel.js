import './platform_config_panel.html';
import { Template } from 'meteor/templating';
import './platform_viewports.js';
import './platform_operating_systems.js';

/**
 * Template Helpers
 */
Template.PlatformConfigPanel.helpers({});

/**
 * Template Event Handlers
 */
Template.PlatformConfigPanel.events({});

/**
 * Template Created
 */
Template.PlatformConfigPanel.onCreated(() => {
  
});

/**
 * Template Rendered
 */
Template.PlatformConfigPanel.onRendered(() => {
  
});

/**
 * Template Destroyed
 */
Template.PlatformConfigPanel.onDestroyed(() => {
  
});
