import './node_recent_renditions.html';
import { Template } from 'meteor/templating';

/**
 * Template Helpers
 */
Template.NodeRecentRenditions.helpers({});

/**
 * Template Event Handlers
 */
Template.NodeRecentRenditions.events({});

/**
 * Template Created
 */
Template.NodeRecentRenditions.onCreated(() => {
  
});

/**
 * Template Rendered
 */
Template.NodeRecentRenditions.onRendered(() => {
  
});

/**
 * Template Destroyed
 */
Template.NodeRecentRenditions.onDestroyed(() => {
  
});
