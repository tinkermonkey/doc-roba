import './reference_nav.html';

import {Template} from 'meteor/templating';

/**
 * Template Helpers
 */
Template.ReferenceNav.helpers({});

/**
 * Template Event Handlers
 */
Template.ReferenceNav.events({});

/**
 * Template Created
 */
Template.ReferenceNav.onCreated(() => {
  
});

/**
 * Template Rendered
 */
Template.ReferenceNav.onRendered(() => {
  
});

/**
 * Template Destroyed
 */
Template.ReferenceNav.onDestroyed(() => {
  
});
