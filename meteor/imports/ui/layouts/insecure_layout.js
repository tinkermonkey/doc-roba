import './insecure_layout.html';

import {Template} from 'meteor/templating';

import '../lib/svg_defs/login_svg_defs.js';

/**
 * Template Helpers
 */
Template.InsecureLayout.helpers({});

/**
 * Template Event Handlers
 */
Template.InsecureLayout.events({});

/**
 * Template Created
 */
Template.InsecureLayout.onCreated(() => {

});

/**
 * Template Rendered
 */
Template.InsecureLayout.onRendered(() => {
  
});

/**
 * Template Destroyed
 */
Template.InsecureLayout.onDestroyed(() => {
  
});
