import './version_servers.html';

import {Template} from 'meteor/templating';

import './components/version_server_list.js';
import './components/version_server_config.js';

/**
 * Template Helpers
 */
Template.VersionServers.helpers({});

/**
 * Template Event Handlers
 */
Template.VersionServers.events({});

/**
 * Template Created
 */
Template.VersionServers.onCreated(() => {
  
});

/**
 * Template Rendered
 */
Template.VersionServers.onRendered(() => {
  
});

/**
 * Template Destroyed
 */
Template.VersionServers.onDestroyed(() => {
  
});
