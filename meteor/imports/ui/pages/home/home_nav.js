import './home_nav.html';

import {Template} from 'meteor/templating';

/**
 * Template Helpers
 */
Template.HomeNav.helpers({});

/**
 * Template Event Handlers
 */
Template.HomeNav.events({});

/**
 * Template Created
 */
Template.HomeNav.onCreated(() => {
  
});

/**
 * Template Rendered
 */
Template.HomeNav.onRendered(() => {
  
});

/**
 * Template Destroyed
 */
Template.HomeNav.onDestroyed(() => {
  
});
