import './web_node_edit_params.html';
import { Template } from 'meteor/templating';
import './edit_node_url_parameters.js';

/**
 * Template Helpers
 */
Template.WebNodeEditParams.helpers({});

/**
 * Template Event Handlers
 */
Template.WebNodeEditParams.events({
  
});

/**
 * Template Created
 */
Template.WebNodeEditParams.onCreated(() => {
  
});

/**
 * Template Rendered
 */
Template.WebNodeEditParams.onRendered(() => {
  
});

/**
 * Template Destroyed
 */
Template.WebNodeEditParams.onDestroyed(() => {
  
});
