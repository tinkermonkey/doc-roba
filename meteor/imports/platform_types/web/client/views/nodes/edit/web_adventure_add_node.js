import './web_adventure_add_node.html';

import { Template } from 'meteor/templating';

/**
 * Template Helpers
 */
Template.WebAdventureAddNode.helpers({});

/**
 * Template Event Handlers
 */
Template.WebAdventureAddNode.events({});

/**
 * Template Created
 */
Template.WebAdventureAddNode.onCreated(() => {
  
});

/**
 * Template Rendered
 */
Template.WebAdventureAddNode.onRendered(() => {
  
});

/**
 * Template Destroyed
 */
Template.WebAdventureAddNode.onDestroyed(() => {
  
});
